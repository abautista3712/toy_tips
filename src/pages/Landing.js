import React, { useState } from "react";
import { Container, Card, InputGroup, Form, Button } from "react-bootstrap";
import "./Landing.css";

const Landing = (props) => {
  return (
    <Container className="LandingWrapper">
      <div>Nav Bar Goes Here</div>
      <div>Carosel Goes Here</div>
    </Container>
  );
};

export default Landing;
